.DS_Store.DS_Store


1. fix carousel for certifications. when first opening the page, the carousel should display the first certification by default. Currently, it may not be showing the first item correctly.
Also when i click on the image when first landing on the page, the lighthouse effect doesnt work until i click on the carousel navigation buttons. The lighthouse effect should work immediately when clicking on the image, even if the carousel is on the first item.